[Unit]
Description=6v6 sysmon-host init — sysmon -i + SSH key + sshd
After=network-online.target

[Service]
Type=simple
ExecStart=/etc/cont-init.d/00-sysmon.sh
Restart=on-failure
RestartSec=5

[Install]
WantedBy=multi-user.target
